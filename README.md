# 久坐提醒 Time Reminder

一个基于 Chrome Extension Manifest V3 的办公久坐提醒插件，用来在专注工作一段时间后提醒你起身活动、喝水或短暂休息。

## 功能特性

- 支持工作、短休息、长休息三段节奏切换
- 支持自定义工作时长、短休息时长、长休息时长
- 支持设置“每几次短休息后进入一次长休息”
- 支持系统通知 + 插件提醒页双提醒方式
- 支持提醒页自动关闭时长配置
- 支持休息倒计时配置
- 支持暂停、恢复、延后 5/10 分钟、跳过本次、测试提醒
- 支持浏览器重启后恢复运行状态
- 支持单实例提醒窗口，避免重复弹出多个提醒页

## 当前界面

- `设置页`：修改提醒节奏、提醒文案、自动关闭时长等配置
- `状态弹窗`：查看当前阶段、剩余时间，快速暂停或恢复
- `提醒页`：到点后执行开始休息、延后提醒、关闭提醒等操作

## 技术方案

- Chrome Extension Manifest V3
- 后台调度：`service worker`
- 定时能力：`chrome.alarms`
- 配置存储：`chrome.storage.sync`
- 运行状态：`chrome.storage.local`
- 测试：Node 24 原生 `node:test`

## 本地使用

### 1. 运行测试

```bash
npm test
```

当前项目测试已迁移到 Node 24 原生测试能力，不依赖额外测试包。

### 2. 以开发者模式加载扩展

1. 打开 Chrome，进入 `chrome://extensions`
2. 打开右上角 `开发者模式`
3. 点击 `加载已解压的扩展程序`
4. 选择仓库根目录：[D:\Documents\time_reminder](D:\Documents\time_reminder)

当前项目可直接从仓库根目录作为 unpacked extension 加载，不依赖额外构建步骤。

## 项目结构

```text
time_reminder/
├─ manifest.json
├─ src/
│  ├─ background/      # 后台调度与窗口管理
│  ├─ shared/          # 常量、校验、存储、计时逻辑
│  ├─ options/         # 设置页
│  ├─ popup/           # 状态弹窗
│  ├─ notification/    # 提醒页
│  └─ assets/icons/    # 扩展图标
└─ tests/              # Node 24 原生测试
```

## 已完成的发布前打磨

- 统一图标资源，并补齐常用尺寸 `16/32/48/128`
- 统一中文文案口径
- 优化提醒页主次层级和自动关闭反馈
- 加固提醒窗口单实例策略
- 修复通知图标加载错误
- 自动化测试已通过

## 测试状态

当前已有自动化测试覆盖：

- 配置校验逻辑
- 工作 / 短休息 / 长休息切换逻辑
- 延后提醒逻辑

最近一次验证结果：

- `2` 个测试文件通过
- `8` 个测试全部通过

## 后续可扩展方向

- 更丰富的提醒文案模板
- 工作时段限制
- 历史提醒记录
- 多套节奏预设

## License

当前仓库未单独声明许可证；如需开源发布，建议补充 `LICENSE` 文件。
