{
  "name": "time-reminder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "echo \"Load this extension as unpacked from the repository root.\""
  }
}
