{
  "name": "time-reminder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "echo \"Load this extension as unpacked from the repository root.\""
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
